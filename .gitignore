.DS_Store
_site/
.jekyll-cache/
.jekyll-metadata
Gemfile.lock
.bundle/
vendor/

# Working data and analysis scripts — excluded from the Jekyll build
# (see _config.yml) and not tracked. Files stay on disk locally.
dat/
py/
